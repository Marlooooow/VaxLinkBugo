APP_DATA_MODE=mock
SUPABASE_URL=https://YOUR_PROJECT.supabase.co
SUPABASE_ANON_KEY=YOUR_PUBLISHABLE_OR_ANON_KEY

# Server-side SMS delivery. Keep real values in the ignored local .env and in
# Supabase Edge Function secrets. Never expose these through Flutter builds.
SEMAPHORE_API_KEY=
SEMAPHORE_SENDER_NAME=
